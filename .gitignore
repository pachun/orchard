# Python bytecode cache. The hooks under configuration/cli/claude/config/hooks/
# are imported/executed by Claude Code and Python writes __pycache__ next
# to them on first run.
__pycache__/
*.pyc

configuration/cli/claude/config/skills/
**/skills/qar/

# The /orchard skill ships with the repo. The global git ignore drops all of
# .claude/ because it's usually per-repo state (settings.local.json,
# history); this lets skills/ through and keeps the rest out.
!.claude/
.claude/*
!.claude/skills/

!.agents/
.agents/*
!.agents/skills
