no bootloader shown / waiting for selection on restart
auto run start-hyprland so we're not left in a plain tty terminal
tui login
hyprland missing things
vim/lua setup (I need to do this manually and slowly)
hyprexpo plugin for mission-control-style workspace overview (must be built against pinned hyprland 0.54.3 — vendor the .so the same way as the hypr .pkgs)
