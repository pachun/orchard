-- Linters to install via Mason and run via nvim-lint. Keys are Mason
-- package names (`:Mason` tab 4); values are the filetypes to lint.
-- nvim-lint expects ft → list-of-linters, so the plugin spec inverts
-- this on setup.
--
-- Example:
--   eslint_d = { "javascript", "typescript", "javascriptreact" },
return {}
