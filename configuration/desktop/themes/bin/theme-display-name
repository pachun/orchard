#!/bin/sh
# Print a theme directory name the way a person would say it:
# catppuccin-frappe -> Catppuccin Frappe. Theme dirs are always
# lowercase-hyphenated, so the reverse mapping is just lowercase + hyphens.
set -eu
printf '%s\n' "$1" | tr '-' ' ' | sed 's/\b\(.\)/\u\1/g'
