-- Window border colors for Everforest Dark. Loaded by the main
-- hyprland.lua through the orchard-themes/active symlink, so a
-- set-theme switch repaints borders to match. Active = the signature
-- everforest green; inactive = a muted surface tone that recedes.
-- Border width itself lives in the main config's general block.
hl.config({ general = { col = { active_border = "rgb(a7c080)", inactive_border = "rgb(4f585e)" } } })
